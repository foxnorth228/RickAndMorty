query getCharacterById($id: ID!) {
    character(id: $id) {
        name
        status
        species
        type
        gender
        origin {
            name
            type
            dimension
        }
        location {
            name
            type
            dimension
        }
        image
    }
}